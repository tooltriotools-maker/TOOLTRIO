/** One-time codemod + reusable guard for removing product-wide FAQ boilerplate. */
import fs from 'node:fs'; import path from 'node:path';
const root=path.resolve('app/calculators');
const patterns=[/completely free/i,/100% free/i,/free to use/i,/no (?:account )?registration/i,/no signup/i,/no subscription/i,/no ads/i,/data (?:stored|shared|sold)/i,/privacy.*data/i,/works (?:on|with) (?:all|any) devices/i,/mobile (?:devices?|friendly)/i,/multiple currencies/i,/what currencies/i,/can i use this calculator for both/i];
function walk(d){let a=[];for(const e of fs.readdirSync(d,{withFileTypes:true})){const f=path.join(d,e.name);if(e.isDirectory())a.push(...walk(f));else if(e.name.endsWith('.tsx'))a.push(f)}return a}
function findMatching(text,start){let depth=0,q=null,esc=false;for(let i=start;i<text.length;i++){const c=text[i];if(q){if(esc)esc=false;else if(c==='\\')esc=true;else if(c===q)q=null;continue}if(c==='"'||c==="'"||c==='`'){q=c;continue}if(c==='[')depth++;else if(c===']'){depth--;if(depth===0)return i}}return -1}
function splitTop(text){let parts=[],start=0,depth=0,q=null,esc=false;for(let i=0;i<text.length;i++){const c=text[i];if(q){if(esc)esc=false;else if(c==='\\')esc=true;else if(c===q)q=null;continue}if(c==='"'||c==="'"||c==='`'){q=c;continue}if(c==='{')depth++;else if(c==='}')depth--;else if(c===','&&depth===0){parts.push(text.slice(start,i+1));start=i+1}}parts.push(text.slice(start));return parts}
let changed=0,removed=0;
for(const file of walk(root)){let text=fs.readFileSync(file,'utf8'),pos=0,out='',fileChanged=false;while(true){const m=text.slice(pos).match(/(?:const|let|var)\s+faqs\s*=\s*\[/);if(!m){out+=text.slice(pos);break}const s=pos+m.index, a=s+m[0].length-1,e=findMatching(text,a);if(e<0){out+=text.slice(pos);break}out+=text.slice(pos,a+1);for(const p of splitTop(text.slice(a+1,e))){if(/^\s*\{/.test(p)&&patterns.some(x=>x.test(p))){removed++;fileChanged=true;continue}out+=p}out+=']';pos=e+1}if(fileChanged){fs.writeFileSync(file,out);changed++}}
console.log(JSON.stringify({filesChanged:changed,removedGenericFaqObjects:removed},null,2));
